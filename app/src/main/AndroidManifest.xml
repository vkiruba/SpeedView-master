<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.github.anastr.speedview">

    <!--
         The ACCESS_COARSE/FINE_LOCATION permissions are not required to use
         Google Maps Android API v2, but you must specify either coarse or fine
         location permissions for the 'MyLocation' functionality. 
    -->

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Home">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".ControlActivity" />
        <activity android:name=".DeluxeSpeedActivity" />
        <activity android:name=".SpeedViewActivity" />
        <activity android:name=".AwesomeSpeedometerActivity" />
        <activity android:name=".RayActivity" />
        <activity android:name=".PointerActivity" />
        <activity android:name=".WorkWithNoteActivity" />
        <activity android:name=".CreateProgrammatically" />
        <activity android:name=".TubeSpeedometerActivity" />
        <activity android:name=".ImageSpeedometerActivity" />
        <activity android:name=".WorkWithIndicatorActivity" />
        <activity android:name=".StartEndDegreeActivity" />
        <activity android:name=".WorkWithModesActivity" />
        <activity android:name=".SpeedTextPositionActivity" />
        <activity android:name=".lineargauge.ProgressiveGaugeActivity" />
        <activity android:name=".lineargauge.ImageLinearGaugeActivity" />
        <activity android:name=".TickActivity" />

        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_maps_key" />

        <activity
            android:name=".MainActivity"
            android:label="title_activity_home"></activity>
    </application>

</manifest>